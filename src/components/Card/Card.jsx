const Card = () => {
  return (
    <div>
      <p>
        Use the search bar bellow to search through Google's Google Book API.
        Click on the book covers to get more details on the title.
      </p>
    </div>
  );
};

export default Card;
